$(`.answers .answer-${<%= @answer.id %>} .errors`).html('<%= render 'shared/errors', resource: @answer %>');

<% if !@answer.errors.present? %>
  $(`.answers .answer-${<%= @answer.id %>}`).html('<%= j render @answer %>')
<% end %>
